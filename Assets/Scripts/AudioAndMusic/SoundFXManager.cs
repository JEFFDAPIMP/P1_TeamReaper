using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//This whole class is for storing references to and using soundFX anywhere it is needed.
public class SoundFXManager : MonoBehaviour
{
    public AudioClip[] playerSoundFX;
    public AudioClip[] antSoundFX;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
